#include<bits/stdc++.h>
using namespace std;
int main()
{
    map<int, int> mp;

    mp.insert({3, 30});
    mp.insert({5, 50});
    mp.insert({4, 40});

    // clear(), erase (), empty(), count(), find(),swap(), upper_bound(),
    // lower_bound(), operator "="
